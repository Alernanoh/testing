//Tabla de multiplicar
let factor = prompt("Ingrese un número: ");

let num = parseInt(factor);

 for (let i=0; i<=9; i++) {
      let mult=(i*num); 
      console.log(`${i}*${num}= ${mult}`);
  }





